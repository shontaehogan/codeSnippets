<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
  </head>
  <a href="/">Home</a>
  <br>


  <div class="">
    <form class="addForm" action="/snippets" method="post">
      <label for="title">Title</label>
      <input type="text" name="title" value="{{title}}" placeholder="Title"/>
      <div class="fieldError">{{errors.title.message}}</div>

      <label for="body">Body</label>
      <textarea type="textarea" row="5" col="5" name="body" value="{{snippets.body}}" placeholder="snippet"/></textarea>
      <div class="fieldError">{{errors.body.message}}</div>

      <label for="notes">Notes</label>
      <input type="text" name="notes" value="{{notes}}" placeholder="notes"/>
      <div class="fieldError">{{errors.notes.message}}</div>

      <label for="language">Language</label>
      <input type="text" name="language" value="{{language}}" placeholder="language"/>
      <div class="fieldError">{{errors.language.message}}</div>
    <div class="">

    <div class="tagsdiv">
      <label for="tags">tags</label>
      <input class='add tags' type="text" id="tags" name="tags" value="{{snippets.tags}}" placeholder="tags"/>
      <div class="fieldError">{{errors.tags.message}}</div>
      <label for="author">author</label>
      <input class='author' type="text" id="author" name="author" value="{{snippets.author}}" placeholder="author"/>
      <div class="fieldError">{{errors.author.message}}</div>


    </div>



  <div class="">
    {{#if snippet._id}}
      <input type="hidden" name="_id" value="{{snippet._id}}" />
    {{/if}}
    <div class="">
    <button class="update" name="add"> Submit </button>

    </div>
  </div>
  </div>
    </form>
  </div>



</script>
  </body>
</html>
